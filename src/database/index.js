/**
 * =============================================================================
 * 数据库管理索引
 * =============================================================================
 *
 * @file       src/database/index.js
 * @module     Database
 * @description
 *   数据库管理模块统一导出。
 *   包含连接池、迁移执行器、重建脚本的入口引用。
 *
 * @author     EduSmart Team
 * @since      v2.0.0
 * @refactored 2026-06-27 - 新增模块，统一数据库管理入口
 *
 * =============================================================================
 */

const pool = require('./connection');

module.exports = {
  pool,
  getConnection: () => pool,
};
